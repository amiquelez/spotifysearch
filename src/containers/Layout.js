import React from 'react';
import Header from '../components/header/Header';
import './Layout.scss';

const Layout = () => {
  return (
    <React.Fragment>
      <Header />
    </React.Fragment>
  );
}

export default Layout;